<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.CommunityMapper">


    <select id="communityList" resultType="com.example.backend.model.Community">
        SELECT
            community_id as communityId,
            title,
            content,
            writer,
            view_count as viewCount,
            created_at as createdAt,
            updated_at as updatedAt
        FROM community
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.example.backend.model.Community">
        INSERT INTO community (title, content, writer, view_count, created_at, updated_at)
        VALUES (#{title}, #{content}, #{writer}, 0, NOW(), NOW())
    </insert>

    <delete id="delete" parameterType="int">
        DELETE FROM community WHERE community_id = #{communityId}
    </delete>

    <select id="selectOne" parameterType="int" resultType="com.example.backend.model.Community">
        SELECT
            community_id as communityId,
            title,
            content,
            writer,
            view_count as viewCount,
            created_at as createdAt,
            updated_at as updatedAt
        FROM community
        WHERE community_id = #{communityId}
    </select>

    <update id="update" parameterType="com.example.backend.model.Community">
        UPDATE community
        SET title = #{title}, content = #{content}, updated_at = NOW()
        WHERE community_id = #{communityId}
    </update>

    <update id="incrementViewCount" parameterType="int">
        UPDATE community
        SET view_count = view_count + 1
        WHERE community_id = #{communityId}
    </update>

</mapper>